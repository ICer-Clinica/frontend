import {
    BarElement, CategoryScale, Legend, LinearScale, Title,
    Tooltip
} from "chart.js";

export const chartJSRegister = {
    CategoryScale,
    LinearScale,
    BarElement,
    Title,
    Tooltip,
    Legend
}